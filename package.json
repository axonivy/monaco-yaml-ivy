{
  "name": "@axonviy/monaco-yaml-ivy",
  "version": "1.0.0",
  "engines": {
    "node": ">=20"
  },
  "volta": {
    "node": "24.11.0"
  },
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "serve": "vite preview",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "sbom": "pnpm dlx @cyclonedx/cdxgen -t pnpm --required-only",
    "sbom:dev": "pnpm dlx @cyclonedx/cdxgen -t pnpm --output bom-dev.json"
  },
  "dependencies": {
    "monaco-languageserver-types": "^0.4.0",
    "monaco-editor": "0.54.0",
    "monaco-yaml": "^5.2.3"
  },
  "devDependencies": {
    "@playwright/test": "1.57.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3",
    "vite": "7.2.7"
  }
}
